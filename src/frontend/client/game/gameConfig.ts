import { GameConfig } from './types.js';

export const gameConfig: GameConfig = {
    boardWidth: 600,
    boardHeight: 360,
    playerWidth: 10,
    playerHeight: 40,
    ballWidth: 10,
    ballHeight: 10,
    ballVelocity: 4,
    playerSpeed: 4
};
